<div class="container">
    {{!-- header  --}}
    <div class="row">
        {{> header}}
    </div>
    {{!-- main content --}}
    <div class="row">
        {{> burgersSection}}
        {{> devouredSection}}
    </div>
    {{!-- add burger form  --}}
    <div class="row">
        {{> burgerForm}}
    </div>
</div>